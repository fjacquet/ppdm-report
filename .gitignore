# dependencies & build output
node_modules/
dist/
dist-ssr/
coverage/
.vite/

# OS / editor
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/

# env / local overrides
.env
.env.*
*.local

# local-only inputs (customer data + scratch — never commit)
/ref/
/data/

# Claude Code local config
.claude/

# brainstorming + visual-check artifacts (never committed)
.superpowers/
.playwright-mcp/
/*.png
